[tool.poetry]
name = "rag-experiments-backend"
version = "0.1.0"
description = ""
authors = ["Isabelle"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
python-dotenv = "^1.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
package-mode = false
